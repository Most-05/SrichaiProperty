# 📢 อ่านก่อนรันโปรเจกต์ — มีของใหม่ที่ต้องอัปเดตเครื่อง

สวัสดีครับ PHmeen — ช่วง 14 ส.ค. - วันนี้ ผมแก้ไปหลายเรื่อง มี 2 อย่างที่กระทบเครื่องคุณโดยตรง
ทำตามขั้นตอนด้านล่างให้ครบก่อนรัน `npm run dev` นะครับ ไม่งั้นจะเจอ error แน่ๆ

**ถ้าเพิ่ง pull ครั้งแรกหลังจากนี้ ทำตามลำดับนี้ทีเดียวจบ:**

```bash
git pull
npm install
npx prisma generate
npx prisma db push
```

(ไฟล์นี้ลบทิ้งได้เลยหลังทำตามเสร็จแล้ว ไม่ต้องเก็บไว้ในโปรเจกต์ถาวร)

---

## 1️⃣ Dependency ใหม่ 3 ตัว (มาจาก PR แผนที่ปักหมุด)

| Package | เวอร์ชัน | ใช้ทำอะไร |
|---|---|---|
| `leaflet` | `1.9.4` | เอนจินแผนที่หลัก |
| `react-leaflet` | `5.0.0` | ตัวห่อให้ใช้กับ React 19 |
| `@types/leaflet` | `1.9.22` | type definitions (dev dependency) |

**ถ้าไม่รัน `npm install`:** จะเจอ error แบบ `Cannot find module 'leaflet'` ทันทีที่เปิดหน้า
ลงประกาศ/แก้ไขประกาศ/รายละเอียดบ้าน (3 หน้านี้ใช้แผนที่จริงแล้ว ไม่ใช่ `<iframe>` ปลอมแบบเดิม)

**ถ้ารันแล้วยังฟ้องว่าหา module ไม่เจอ:** ให้ Restart TS Server
(Ctrl+Shift+P → พิมพ์ "TypeScript: Restart TS Server") เหมือนกับตอนแก้ schema.prisma

---

## 2️⃣ เพิ่ม 4 คอลัมน์ในตาราง `properties` (สำคัญกว่า — ต้องรัน 2 คำสั่งข้างล่าง)

| คอลัมน์ | ชนิด | เก็บอะไร |
|---|---|---|
| `common_fee` | `Decimal?` | ค่าส่วนกลางรายเดือน |
| `parking_spaces` | `Int?` | จำนวนที่จอดรถ |
| `floors` | `Int?` | จำนวนชั้น |
| `ownership_type` | `String?` | กรรมสิทธิ์ ("ขายขาด (Freehold)" ฯลฯ) |

**ทุกคอลัมน์เป็น nullable ทั้งหมด** — เพิ่มแล้วไม่กระทบข้อมูลเดิมที่มีอยู่แล้วเลย (ตรวจสอบแล้วจริง
ก่อน push: `migrate diff` มีแค่ `ADD COLUMN` 4 บรรทัด ไม่มี `DROP`/`ALTER` ใดๆ)

**ต้องรันทั้ง 2 คำสั่งนี้ตามลำดับ ไม่งั้นเครื่องคุณจะพัง:**
```bash
npx prisma generate   # อัปเดต Prisma Client ให้รู้จักคอลัมน์ใหม่ (แก้ TS error "property does not exist")
npx prisma db push    # สร้างคอลัมน์จริงใน DB ในเครื่องคุณ (แก้ error "column does not exist")
```

**บริบท:** ฟอร์มลงประกาศเก็บ 4 ค่านี้ไว้นานแล้ว (มี validation ด้วย) แต่ไม่เคยมีคอลัมน์รองรับ
ข้อมูลเลยหายทุกครั้งที่นายหน้ากรอก — รอบนี้อุดบั๊กให้ครบทั้งวงจร (DB → API → ฟอร์ม 2 หน้า → แสดงผล)

---

## เช็คว่าทำครบหรือยัง

รันคำสั่งนี้ ถ้าไม่มี error แปลว่าเรียบร้อย:
```bash
npx tsc --noEmit
```

มีคำถามหรือติดตรงไหนทักมาได้เลยครับ
